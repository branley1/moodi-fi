{
  "name": "moodi-fi",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.7.9",
    "cookie-parser": "^1.4.7",
    "mongoose": "^8.8.4",
    "react": "19.0.0", 
    "react-dom": "19.0.0", 
    "react-router-dom": "^6.22.1",
    "web-vitals": "^4.2.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build-frontend": "react-scripts build",
    "deploy-frontend": "rsync -av --delete build/ ../backend/public/",
    "build-and-deploy": "npm run build-frontend && npm run deploy-frontend"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11",
    "react-scripts": "5.0.1"
  }
}